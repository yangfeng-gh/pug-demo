//- 分支传递 (Case Fall Through)。 您可以像 JavaScript 中的 switch 语句那样使用传递（fall through）。

- var friends = 0
case friends
    when 0
    when 1
        p 您的朋友很少
    default
        p 您有 #{friends} 个朋友

//- 不同之处在于，在 JavaScript 中，传递会在明确地使用 break 语句之前一直进行。而在 Pug 中则是，传递会在遇到非空的语法块前一直进行下去。

